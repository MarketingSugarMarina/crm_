# .env.example — คัดลอกเป็น .env แล้วแก้ค่าตามจริง

PORT=3000
NODE_ENV=development

# Railway: วาง DATABASE_URL ที่ได้จาก Railway dashboard ตรงนี้
# รูปแบบ: postgres://user:password@host:port/dbname
DATABASE_URL=postgres://postgres:password@localhost:5432/crm_db
